apiVersion: v1
kind: Service
metadata:
  name: jenkins-cicd-service
  namespace: jenkins-cicd
spec:
  type: NodePort
  selector:
    app: jenkins-cicd-app
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
